# tone-ladder

An educational tool for generating musical tone ladders.


## AI declaration

*GPT-5 mini* have been used in `src/pages/Home.tsx` and `src/styles/svg.css` to tweek the CSS and styling of the SVGs and their containers on the home page to make them scale correctly.